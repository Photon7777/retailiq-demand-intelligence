# Local Model Artifacts

Phase 2 training writes local model artifacts here:

- `retailiq_forecast_model.pkl`
- `forecast_metrics.json`

These generated files are ignored by Git so secrets, local paths, and large binary artifacts do not get committed.
